import { ProjectIdToNamePipe } from './project-id-to-name.pipe';

describe('ProjectIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
